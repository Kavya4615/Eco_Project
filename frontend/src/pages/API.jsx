import { Box, Typography } from "@mui/material";

function API() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Air Quality API
      </Typography>
      <Typography>
        Information about our AQI API and developer integration.
      </Typography>
    </Box>
  );
}

export default API;