import { Box, Typography } from "@mui/material";

function AppDownload() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>
        Air Quality App
      </Typography>
      <Typography>
        Download our mobile app to monitor air quality on the go.
      </Typography>
    </Box>
  );
}

export default AppDownload;